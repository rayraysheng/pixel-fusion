import React from 'react';
import ReactDOM from 'react-dom';
// import './index.css'; // Make sure to create this CSS file or remove this import if you don't need it
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
